export type User = {
    username: string;
    last?: string;
    last_kana?: string;
    first?: string;
    first_kan?: string;
    icon_url?: string;
    status_text?: string;
    background_url?: string;
    intro_text?: string;
    private_memo?: string;
    use_line?: boolean;
    use_discord?: boolean;
    use_twitter?: boolean;
    use_github?: boolean;
};
